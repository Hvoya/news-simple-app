import { createStyles } from '@material-ui/core/styles';

function styles() {
  return createStyles({
    container: {
      maxWidth: '66em',
      display: 'flex',
      flexDirection: 'column',
    },
  });
}

export default styles;
